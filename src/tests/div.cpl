count, first: int;
second: int;

{
     input (first);
     input (second);
     count = 0;

     while (first >= second) {
       first = first-second;
       count = count+1;
     }

     output(count);
}
